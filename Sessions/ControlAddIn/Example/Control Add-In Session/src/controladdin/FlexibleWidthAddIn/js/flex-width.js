document.addEventListener('DOMContentLoaded', (event) => {
    const controlAddIn = document.querySelector('#controlAddIn');
    const textEl = createTextElement('Flexible Width');
    controlAddIn.append(textEl);
});